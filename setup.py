from setuptools import setup

setup(name='dns_packet',
      version='0.1',
      description='DNS packet creation',
      url='http://github.com/killswitch-GUI/dns_packet',
      author='Alexander Rymdeko-Harvey',
      author_email='lolyearight@cybersyndicates.com',
      license='GNU 3.0',
      packages=['dns_packet'],
      install_requires=[
          '',
      ],
      zip_safe=False)